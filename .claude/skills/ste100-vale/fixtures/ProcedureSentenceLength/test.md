# Test

Install the panel and check the bolts and inspect the wires and test the switch and verify the connection now please.

Install the panel and check the bolts now.

Set the read-write timeout value (see the guide for defaults) to 30 ms before you start the pump and the built-in fan today.

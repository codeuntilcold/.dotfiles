autoload -U compinit
compinit -i

plugins=(git systemd)

### envs
export ZSH="$HOME/.oh-my-zsh"

export KEYTIMEOUT=1

HISTSIZE=999999999
SAVEHIST=$HISTSIZE

export EDITOR=nvim
export MANPAGER='nvim +Man!'

PATH="$HOME/.asdf/shims:$PATH"
PATH="$HOME/.local/bin/:$PATH"
PATH="$HOME/.local/bin/statusbar-lukesmithxyz/:$PATH"
PATH="$HOME/.local/scripts:$PATH"
PATH="/usr/local/go/bin:$PATH"
PATH="$HOME/go/bin:$PATH"
PATH="$HOME/.foundry/bin:$PATH"
PATH="$HOME/flutter/bin:$PATH"
PATH="$HOME/.pub-cache/bin:$PATH"
# PATH="$HOME/Downloads/zips/platform-tools:$PATH"

export PATH

export DEV_HOME="$HOME/dev"

alias hr='heroku'
alias flatpak-flutter='docker run --rm -v "$PWD":/usr/src/flatpak -u `id -u`:`id -g` theappgineer/flatpak-flutter:latest'

alias upgrade='sudo apt update && sudo apt upgrade && sudo snap refresh && flatpak update -y'

alias v="nvim"
alias vv="nvim"
alias vg="nvim +G"
alias ez='nvim ~/.zshrc'
alias sz='source ~/.zshrc'
alias ck='nvim ~/.sdai/cookies'
alias tma='tmux -2 a'
alias mkdir='mkdir -v'
alias lg='lazygit'
alias ldk='lazydocker'
alias dbui='nvim -c "DBUI"'
alias oil='nvim -c "Oil"'
alias pbcopy='xclip -sel c'
alias pbpaste='xclip -sel c -o'
alias nr='newsraft'

### suffer from slop
alias cl='claude'
alias cr='claude -r'
alias cls='claude --model sonnet --effort max'

# NOTE: completion would be cool and avoid these things completely
function gdiff() {
  local current
  local guess
  local branch

  current=$(git branch --show-current)
  guess=$(git reflog show "$current" --format='%gs' | tail -1 | sed 's@branch: Created from origin/@@')
  branch=${1:-$guess}
  [[ "$branch" != "$guess" ]] && shift
  git diff -w $(git merge-base @ $branch) --stat -p "$@"
}

# usage: pn [client | api | domain] command
function pn() {
  if [[ $# -eq 0 ]]; then
    pnpm
    return
  fi

  case "$1" in
    cl | client)
      shift
      pnpm -F client "$@"
      return
      ;;
    a | api)
      shift
      pnpm -F api "$@"
      return
      ;;
    d | domain)
      shift
      pnpm -F domain "$@"
      return
      ;;

    dev)
      fuser -k 3000/tcp 8000/tcp 2>/dev/null
      pnpm dev "$@"
      # To summarize what was done to make port 3001 work:
      # 1. Ran pnpm codegen
      # 2. Backend only allowed localhost:3000. Added a Vite proxy so the browser calls :3001 and Vite forwards to :8000
      # 3. Updated .env: VITE_API_BASE_URL=http://localhost:3001/graphql + VITE_API_PROXY_TARGET=http://localhost:8000
      return
      ;;

    start-cc-dev)
      mkdir rules/
      ln -s ~/dev/datum-monorepo/rules/ rules
      ln -s ~/dev/datum-monorepo/sgconfig.yml sgconfig.yml
      ln -s ~/dev/datum-monorepo/.nvim.lua .nvim.lua
      ln -s ~/dev/datum-monorepo/.mcp.json .mcp.json
      ln -s ~/dev/datum-monorepo/.claude/CLAUDE.md .claude/
      ln -s ~/dev/datum-monorepo/.claude/settings.local.json .claude/
      ln -s ~/dev/datum-monorepo/.claude/commands/master-worktree-init.md .claude/commands/master-worktree-init.md
      ln -s ~/dev/datum-monorepo/.claude/commands/start.md .claude/commands/start.md
      ln -s ~/dev/datum-monorepo/.claude/commands/start-new-day.md .claude/commands/start-new-day.md
      ln -s ~/dev/datum-monorepo/.claude/commands/rodney-datum.md .claude/commands/rodney-datum.md
      ln -s ~/dev/datum-monorepo/infrastructure/live/.terraform/ infrastructure/live/.terraform
      pn i
      pn -F './apps/*' extract-env-manifest
      pn -F env generate-env development
      pn -F env generate-env test
      pn d build
      pn codegen
      return
      ;;
  esac

  pnpm "$@"
}

. "$HOME/.cargo/env"

export FZF_DEFAULT_OPTS="--layout=reverse"
### endenvs

source $ZSH/oh-my-zsh.sh

# Edit commands using vi bindings -- better than `set -o vi`
bindkey -v

function y() {
  local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
  yazi "$@" --cwd-file="$tmp"
  if cwd="$(command cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
    builtin cd -- "$cwd"
  fi
  rm -f -- "$tmp"
}

# For fzf
source /usr/share/doc/fzf/examples/key-bindings.zsh
source /usr/share/doc/fzf/examples/completion.zsh

function __tmux_fzf_autocomplete__() {
  tmux capture-pane -pS -100000 \
    | awk 'BEGIN { RS = "[ \t\n]" } length($0) > 4 && !seen[$0]++' \
    | fzf --no-sort --exact +i --tac --layout reverse --height 20
}

function __tmux_fzf_autocomplete_widget__() {
  LBUFFER+="$(__tmux_fzf_autocomplete__)"
  zle redisplay
}

zle -N __tmux_fzf_autocomplete_widget__
bindkey '^n' __tmux_fzf_autocomplete_widget__

# Automatically create a new session
# tmux has-session -t main 2> /dev/null || tmux new-session -d -s main

eval "$(zoxide init zsh)"
[[ -z "$STARSHIP_SHELL" ]] && eval "$(starship init zsh)"

# OSC 133 prompt marks for tmux copy-last-output (prefix+O); starship won't emit them (starship#5463).
# A must ride inside PROMPT: a precmd-printed mark dies when zle redraws the prompt line.
if [[ -z "$__osc133_done" ]]; then
  __osc133_done=1
  PROMPT=$'%{\e]133;A\e\\%}'"$PROMPT"
  __osc133_c() { print -n '\e]133;C\e\\' }
  preexec_functions+=(__osc133_c)
fi

eval "$(gh completion --shell zsh)"
complete -C '/usr/local/bin/aws_completer' aws
eval "$(kubectl completion zsh)"
eval "$(helm completion zsh)"
eval "$(hcloud completion zsh)"
